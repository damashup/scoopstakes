const INITIAL_STATE =  {
    latestSeason : "1819"
}

const seasonReducer = (state=INITIAL_STATE, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default seasonReducer;